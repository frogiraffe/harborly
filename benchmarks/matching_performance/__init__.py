"""Performance profile for `PortRegistry.match_names` under bulk CSV workloads.

Companion to `benchmarks.matching_accuracy` (which scores correctness). This
package only measures latency and throughput; it does not score matching
correctness and it does not change matching behavior.
"""
