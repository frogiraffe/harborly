## Summary

What this change does and why.

## Changes

-

## Checklist

- [ ] Tests cover the change and pass locally (`uv run pytest -q`)
- [ ] `ruff format --check`, `ruff check`, and `mypy src` pass
- [ ] Public names and commands stay documented
- [ ] Public API, CLI, and JSON changes follow `CONTRIBUTING.md#public-compatibility-contract`
- [ ] `uv run python scripts/check_docs.py` passes (root README links stay
      absolute — it is rendered as the PyPI long description)
